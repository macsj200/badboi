var featureService = require('./feature-service.js');
var log = require('tracer').console();

var outfitService = {
	getOutfit: function(req, segment, queryObj, callback) {
	}
}

module.exports = outfitService;
